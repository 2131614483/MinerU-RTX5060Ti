---
tags:
  - MinerU
  - GPU适配
  - 项目说明
  - RTX5060Ti
  - claude
date: 2026-08-10
---

# 项目说明 — MinerU-RTX5060Ti

> 本项目研究 **MinerU 文档解析在 RTX 5060 Ti / RTX 50 系（Blackwell）上的适配**，并沉淀全系列显卡 × CUDA 配置组合总表。
> 目录：[[MinerU-RTX5060Ti-适配报告-详细]] ｜ [[MinerU-RTX5060Ti-适配报告-简略]] ｜ [[MinerU-版本-显卡-CUDA-配置矩阵]]

## 项目背景

RTX 5060 Ti 是 Blackwell 架构（sm_120，compute capability 12.0）。按官方 git 方式安装 MinerU 时，pip 会解析到默认的 cu126（CUDA 12.6）PyTorch 构建——而 **sm_120 只有 CUDA ≥ 12.8 才带内核**，导致 GPU 用不起来（`is_available()=False` 或 `no kernel image`）。

**成功的关键：显式安装 PyTorch 的 cu128 构建**（`torch>=2.7.0+cu128`）。

MinerU 本身（2.x–3.x）从不锁定 CUDA 构建，只声明 `torch>=2.6.0,<3`——CUDA 版本完全由实际安装的 torch 决定（官方 changelog："cuda版本由torch决定"）。

## 交付物

| 文档 | 内容 |
|---|---|
| [[MinerU-RTX5060Ti-适配报告-详细]] | RTX 5060 Ti 适配完整研究：证据链、官方失败原因、本机实测、复现方法 |
| [[MinerU-RTX5060Ti-适配报告-简略]] | 一句话结论 + 复现命令的快速参考卡 |
| [[MinerU-版本-显卡-CUDA-配置矩阵]] | MinerU 各版本 × 全系列显卡 × CUDA 配置组合总表（实测自 PyPI 与 PyTorch 官方索引） |

## 仓库与本地地址

| 项 | 地址 |
|---|---|
| GitHub 仓库 | https://github.com/2131614483/MinerU-RTX5060Ti（私有，main） |
| 本地项目目录 | `D:\MinerU-RTX5060Ti` |
| Obsidian 目录 | `研究/MinerU-RTX5060Ti/`（本 vault） |
| 批处理脚本仓库 | https://github.com/2131614483/MinerU-BatchScripts（私有，main） |
| 脚本本地目录 | `D:\MinerU-BatchScripts` |
| 脚本来源 | `D:\MinerU-master\脚本.py`、`D:\MinerU-master\脚本1.py` |

## 关键复现命令

```bash
# RTX 50 系必须显式装 cu128 构建
pip install torch==2.7.1+cu128 torchvision==0.22.1+cu128 torchaudio==2.7.1+cu128 \
  --index-url https://download.pytorch.org/whl/cu128

# 验证
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_capability(0))"
# 期望输出：True (12, 0)
```

## 同步约定

本项目与 GitHub 仓库 `MinerU-RTX5060Ti` 一一对应：**仓库名 = Obsidian 文件夹名**，报告同步自本目录（见记忆 [[obsidian-github-sync-convention]]）。本目录后续新增报告时，同步到仓库时一并推送。
